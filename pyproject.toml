[project]
name = "my-little-chatgpt"
version = "0.1.0"
description = "A terminal AI agent with a basic tool-calling loop."
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12.0",
    "httpx>=0.27.0",
    "lxml>=5.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
